# Normalize line endings to LF in the repo regardless of the cloning
# machine's core.autocrlf, so bridge.py/test_bridge.py (edited on Windows,
# deployed to a Linux host) and sentinel.ps1 (Windows-only) don't produce
# noisy whitespace-only diffs between contributors.
* text=auto eol=lf

*.png binary
*.webp binary
